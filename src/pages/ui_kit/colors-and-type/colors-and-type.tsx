import React from 'react';
import './colors-and-type.scss';

export default class ColorsAndType extends React.Component {
  render(): React.ReactNode {
    return (
      <h1>Страничка Colors and type</h1>
    );
  }
}